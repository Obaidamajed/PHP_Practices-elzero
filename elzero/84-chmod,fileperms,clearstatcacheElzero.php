<?php

  /*
    File System Functions
    - chmod(File[Required], Mode[Required])
    --- Change Mode
    - fileperms(Name)
    --- Gets File Permissions
    - clearstatcache()
    --- Clear Cache

    Permissions: الإذن
    - You Can Change Owner chown()
    - You Can Change Group chgrp()
  */

  // mkdir("Y", 0700);
  // echo fileperms("Y") . "<br>"; // برجعلي الفايل بيرمشن, برجعلي نيوماريك فاليو
  // chmod("Y", 0644); // بقدر أغير رقم الفايل بيرمشن **** مو فاهم من هون
  // clearstatcache();
  // echo fileperms("Y") . "<br>";
